Place your headshot here as `profile.jpg` (or profile.png, profile.webp — just
update the <img src> in ../index.html if you change the extension).

Recommended: 800 × 1000 px, 4:5 ratio, ~100–200 KB JPEG.

If no image is present the hero shows a polished monogram placeholder ("AK"),
so this is entirely optional.

Also drop your CV here as `Ali_Karami_CV.pdf` to enable the "Download CV" button.
